/* eslint-disable no-unused-vars */

declare type INewUser = {
  firstName: string;
  lastName: string;
  email: string;
  password: string;
};

declare type IUser = {
  id: string;
  firstName: string;
  lastName: string;
  email: string;
};
